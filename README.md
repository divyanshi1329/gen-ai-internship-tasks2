task2_seo_blog_tool (TASK 2)

Pipeline: Product scraping/API → SEO keyword research → 150–200 word blog generation → published on WordPress
📝 Published Blog Links:

<BLOG_LINK_1>

<BLOG_LINK_2>

<BLOG_LINK_3>
... (all 10 links)
📁 Outputs: Blog content JSON + HTML export + report included.

## Setup
1. Clone the repo
2. Create `.env` file using `.env.example`
3. Install requirements:
   pip install -r requirements.txt
4. Run:
   python main.py
